#pragma once
#include <stdio.h>

// COORDINATE SYSTEM.
// We work with 10px = 1m.
// Thus the space looks like 160m * 100m.
const float PX_TO_M = 0.1f;
const float M_TO_PX = 10.0f;

const size_t MAX_BODIES = 64;